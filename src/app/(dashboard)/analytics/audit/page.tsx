import { ComingSoon } from "@/components/cr";
import { FileSearch } from "lucide-react";

export default function AnalyticsAuditPage() {
  return (
    <ComingSoon
      icon={FileSearch}
      title="Audit Logs"
      description="Audit logs are coming soon."
      features={[]}
    />
  );
}
