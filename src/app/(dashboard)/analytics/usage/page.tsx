import { ComingSoon } from "@/components/cr";
import { FileText } from "lucide-react";

export default function AnalyticsUsagePage() {
  return (
    <ComingSoon
      icon={FileText}
      title="Usage"
      description="Usage analytics are coming soon."
      features={[]}
    />
  );
}
